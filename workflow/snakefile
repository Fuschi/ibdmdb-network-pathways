import pandas as pd


# -----------------------------------------------------------------------------
# Setup
# -----------------------------------------------------------------------------

configfile: "workflow/config/config.yaml"

metadata = pd.read_csv(config["metadata"], sep="\t", dtype=str)

RUNS = metadata["run_accession"].tolist()
WORK = "workflow/work"

CONTAINERS = config["containers"]
REFERENCES = config["references"]

# -----------------------------------------------------------------------------
# Target rule
# -----------------------------------------------------------------------------

rule all:
    input:
        "results/clean_paired_seqkit_stats.tsv",
        "results/kneaddata_summary.tsv",
        expand("workflow/work/metaphlan/{run}.metaphlan.tsv", run=RUNS)

# -----------------------------------------------------------------------------
# Rules
# -----------------------------------------------------------------------------

include: "rules/download.smk"
include: "rules/raw_qc.smk"
include: "rules/kneaddata.smk"
include: "rules/clean_qc.smk"
include: "rules/metaphlan.smk"
